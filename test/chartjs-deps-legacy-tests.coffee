chai = require 'chai'
sinon = require 'sinon'

{Chartjsdeps-legacy} = require '../src/chartjs-deps-legacy'

chai.should()

describe 'chartjs-deps-legacy', ->
  it 'should do something', ->
    true.should.equal true